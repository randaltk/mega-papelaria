import ProductList from "@/components/productsList/ProductsList";

const Producer = () => {
  return (
    // <ProducerProvider>
     <ProductList/>
    // </ProducerProvider>
  );
};

export default Producer;
